{
  "name": "root",
  "private": true,
  "scripts": {
    "app": "yarn --cwd packages/app start",
    "deploy": "yarn --cwd packages/server deploy",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.4.3"
  }
}
